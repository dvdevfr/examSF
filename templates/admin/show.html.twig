{% extends 'base.html.twig' %}

{% block title %}Employé
{% endblock %}

{% block body %}
	<h1>Employé</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ user.id }}</td>
			</tr>
			<tr>
				<th>Email</th>
				<td>{{ user.email }}</td>
			</tr>
			<tr>
				<th>FirstName</th>
				<td>{{ user.firstName }}</td>
			</tr>
			<tr>
				<th>LastName</th>
				<td>{{ user.lastName }}</td>
			</tr>
			<tr>
				<th>ProfilePicture</th>
				<td>{{ user.profilePicture }}</td>
			</tr>
			<tr>
				<th>Sector</th>
				<td>{{ user.sector }}</td>
			</tr>
			<tr>
				<th>ContractType</th>
				<td>{{ user.contractType }}</td>
			</tr>
			<tr>
				<th>ContractExpirationDate</th>
				<td>{{ user.contractExpirationDate ? user.contractExpirationDate|date('Y-m-d') : '' }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_admin_index') }}">back to list</a>

	<a href="{{ path('app_admin_edit', {'id': user.id}) }}">edit</a>

	{{ include('admin/_delete_form.html.twig') }}
{% endblock %}
